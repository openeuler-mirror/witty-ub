# Core web server
fastapi==0.120.1
uvicorn==0.49.0

# Scheduling & utilities
apscheduler==3.10.4
python-multipart==0.0.20

# HTTP clients / async I/O
aiohttp==3.9.5
aiofiles==25.1.0
requests==2.32.2
httpx==0.28.1
jsonschema>=4.20.0,<5.0.0
mcp>=1.28.0,<2.0.0
urllib3>=2.1.0

# Encoding / archive
chardet==4.0.0
rarfile>=4.0

# MCP server support
mcp>=1.28.0,<2.0.0

# Data validation
pydantic>=2.12.3,<2.14.0

# TOML config support (tomllib is built-in on Python >= 3.11)
tomli>=2.0.0; python_version < "3.11"

# PostgreSQL-only backend (SQLAlchemy 2.x + asyncpg)
sqlalchemy==2.0.51
asyncpg==0.31.0

# Numerical computation (bucket percentile statistics, top-level import)
numpy>=1.26.0,<3.0.0
polars>=1.0

# Testing (optional, install when running pytest)
pytest>=8.0.0
pytest-asyncio>=0.23.0
